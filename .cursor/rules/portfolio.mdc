---
description: Samiul portfolio API conventions — read skills.md before editing
alwaysApply: true
---

# Samiul portfolio API context

Before scaffolding or editing code in this repository:

1. Read and follow [`skills.md`](../../skills.md) at the repo root.
2. For agent skill entry, use `.cursor/skills/portfolio/`.
3. This repo has **not** been restructured yet, so existing files under `src/controllers/` and `src/routes/` are the legacy shape. Follow the target shape documented in `skills.md` (`src/modules/<domain>/` with routes → controller → service → serializer) instead of copying what is there.
4. Target runtime is **Koyeb with Docker**, not Vercel serverless. `app.listen` stays; do not add serverless handlers.
5. Zod lives in `src/shared` and is independent from the client repo's copy — update both sides by hand after a contract change.
6. No Redis. Auth stays Bearer until a task explicitly asks for the httpOnly cookie migration.
