meta {
  name: Contact
}

auth {
  mode: none
}

docs {
  Public, and the only endpoint that sends mail. Delivered with Resend to `ADMIN_EMAIL`, with `replyTo` set to the visitor so a reply from the inbox reaches them directly. Nothing is stored in MongoDB.
}
